html
    head
        style
            include orderstatus.css
    body
        h1(id="top") Welcome to Online Pharmacy
        div(id="line")
            p Hello, #{name}
        div(id="main")
            h1 Order Status
            p #{message}
        div(id="bottom")
            a(href='/logout' class="btmbtns") Log Out
            a(href='/page' class="btmbtns") Go Back